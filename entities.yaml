entities:

  construction:

    menu: >
      (
        [
          { "location": Заказы,
            "link": "/entities/construction/orders"
          }
        ]
      )

    presentations:

      orders:
        type: markdown
        template: orders.md
        source: >
          (
          
          )